[phases.setup]
nixPkgs = ['nodejs_18', 'npm-9_x']

[phases.install]
cmds = ['npm ci']

# Skip build phase entirely
[phases.build]
cmds = ['echo "Skipping build - using tsx directly"']

[start]
cmd = 'node railway-simple-start.js'
